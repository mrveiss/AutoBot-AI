# NPU Worker Dependencies
# Optimized for Intel NPU acceleration

# Core Framework
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
pydantic>=2.13.4

# Intel OpenVINO for NPU acceleration
# #14453: openvino-dev dropped -- it is a deprecated meta-package frozen at
# 2024.6.0 with no release compatible with openvino 2026.x, so a resolve
# against this file backtracked into a numpy with no cp314 wheel and died in
# an sdist build (the same defect fixed in the npu-worker role by #14449).
# #15408: floor no longer restated here -- constraints/shared.txt (below) is
# the single source of truth, so a dependabot bump there needs no edit here.
openvino  # version from constraints/shared.txt

# Model and Tokenization (lightweight)
tokenizers>=0.22.2,<0.24  # #10331: transformers caps tokenizers<=0.23.0, so a floor above that is
                          # unsatisfiable. #10416 (a grouped dependency bump) raised it back to
                          # >=0.23.1 two days after #10332 fixed it, keeping this comment, and the
                          # ai-stack + npu-worker installs have failed to resolve ever since.
                          # scripts/tokenizers_transformers_range_test.py now fails on a floor
                          # that excludes 0.23.0.
transformers>=5.16.1
huggingface-hub>=1.29.0

# Tensor Operations (NPU optimized)
-c ../../../constraints/shared.txt  # single source of truth for shared versions (#10524)
numpy  # version from constraints/shared.txt
# HTTP and API
aiohttp>=3.14.3  # GH#8323: zip bomb, request smuggling CVEs
httpx>=0.28.1
requests>=2.34.2

# Utilities
python-dotenv>=1.2.3
structlog>=26.1.0
tenacity>=9.1.4
async-timeout>=5.0.1

# Redis client for communication
redis>=8.1.0

# Development and Testing
pytest>=9.1.1
pytest-asyncio>=1.4.0
