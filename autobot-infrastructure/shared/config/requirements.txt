# ===== CORE FRAMEWORK DEPENDENCIES =====
fastapi>=0.135.2
starlette>=1.0.0                 # SECURITY UPDATE - CVE fix (GHSA-7f5h-v6xp-fcq8)
python-multipart>=0.0.22          # Required for FastAPI form data handling
uvicorn>=0.42.0
requests==2.33.0
aiohttp==3.13.3
aiofiles>=25.1.0
aiosqlite>=0.22.1
beautifulsoup4>=4.14.3
pyyaml==6.0.3
pydantic>=2.12.5
pydantic-settings>=2.13.1          # Pydantic v2 settings management (Issue #768)
websockets>=16.0

# ===== MULTI-AGENT ARCHITECTURE DEPENDENCIES =====
# LLM & AI Framework Support
openai>=2.30.0                    # OpenAI API support for future cloud models
anthropic>=0.86.0                 # Claude API support for future integration
tiktoken>=0.12.0                  # Token counting for different models
transformers>=5.4.0             # HuggingFace transformers for model utils - SECURITY UPDATE
sentence-transformers>=5.3.0     # Embedding models support
torch>=2.11.0                     # PyTorch for model operations - compatible version
torchvision>=0.26.0              # PyTorch vision models for CLIP and image processing
torchaudio>=2.11.0                # PyTorch audio models for Whisper and audio processing
accelerate>=1.13.0               # HuggingFace model optimization and GPU acceleration

# Agent Communication & Coordination
asyncio-mqtt>=0.16.2             # MQTT for agent communication (future)
celery>=5.6.3                    # Task queue for agent coordination - version bump for IaC platform
kombu>=5.6.2                     # Messaging for Celery
ansible-runner>=2.4.3            # Ansible playbook execution with event streaming for IaC

# Memory & Storage
redis>=7.4.0                  # Redis for agent state and coordination
chromadb>=1.5.5                  # Vector database for RAG agents
# sqlite3                        # Built-in Python module, not a pip package
sqlalchemy>=2.0.48                # Database ORM for advanced storage

# ===== SPECIALIZED AGENT DEPENDENCIES =====
# System Commands Agent
psutil>=7.2.2                    # System information and process management
sh>=2.2.2                        # Shell command execution wrapper
# subprocess32>=3.5.4              # Not needed in Python 3.3+, subprocess is built-in

# Research Agent & Web Integration
selenium>=4.41.0                 # Web automation (backup to Playwright)
playwright>=1.58.0               # Primary web automation
httpx>=0.28.1                    # Async HTTP client for research
newspaper3k>=0.2.8               # Article extraction
readability>=0.3.2               # Content extraction
python-magic>=0.4.27             # File type detection

# RAG Agent & Knowledge Processing
python-docx>=1.2.0               # Document processing
pypdf>=6.9.2                     # PDF processing - SECURITY UPDATE (improved permission checks)
python-pptx>=1.0.2              # PowerPoint processing
openpyxl>=3.1.5                  # Excel processing
markdown>=3.10.2                  # Markdown processing
pytesseract>=0.3.13              # OCR for image text extraction

# Chat Agent & NLP
nltk>=3.9.4                      # Natural language processing
spacy>=3.8.13                     # Advanced NLP (optional)
textstat>=0.7.13                  # Text complexity analysis

# ===== GPU-ACCELERATED MULTI-MODAL AI DEPENDENCIES =====
# Audio Processing & Speech Recognition
librosa>=0.11.0                  # Audio preprocessing and feature extraction
scipy>=1.17.1                    # Scientific computing for audio processing

# ===== GUI & AUTOMATION =====
pyautogui==0.9.54
mouseinfo==0.1.3
pillow>=12.1.1
numpy>=2.4.3,<3.0.0
opencv-python>=4.13.0.92          # Computer vision for GUI automation
pygetwindow>=0.0.9               # Window management

# ===== EXISTING INTEGRATIONS =====
markdownify>=1.2.2              # SECURITY UPDATE - CVE fix (GHSA-7mpr-5m44-h73r)
langchain>=1.2.13
langchain-community>=0.4.1
langchain-experimental>=0.4.1      # For advanced LangChain features
llama-index>=0.14.19,<0.15.0
llama-index-llms-ollama>=0.10.1,<1.0.0
llama-index-embeddings-ollama>=0.9.0,<1.0.0
llama-index-vector-stores-redis>=0.8.0,<1.0.0
llama-index-readers-file>=0.6.0,<1.0.0   # File readers for RAG - 0.6.0+ for core 0.13.0 + pypdf 6.x compatibility

# ===== VOICE INTERFACE DEPENDENCIES =====
# Moved to requirements-voice.txt to avoid CI/CD build failures
# Install separately: pip install -r requirements-voice.txt
# These require system audio libraries not available in all environments

# ===== SECURITY & DEVELOPMENT TOOLS =====
pre-commit>=4.5.1
detect-secrets>=1.5.0
pycryptodome>=3.23.0              # Encryption for secure agent communication
cryptography>=46.0.6             # Additional crypto support - SECURITY UPDATE
pyjwt>=2.12.1                     # JWT token authentication
bcrypt>=5.0.0                    # Password hashing

# ===== MONITORING & OBSERVABILITY =====
prometheus-client==0.24.1         # Prometheus metrics collection for timeout tracking
structlog>=25.5.0                # Structured logging
rich>=14.3.3                     # Enhanced console output
tqdm==4.67.3                     # Progress bars for long operations - pinned

# ===== CONFIGURATION & UTILITIES =====
python-dotenv>=1.2.2             # Environment variable management
click>=8.3.1                     # CLI interface utilities
jsonschema>=4.26.0               # JSON schema validation
validators>=0.35.0               # Input validation utilities
python-json-logger>=4.0.0
paramiko>=4.0.0                   # SSH client library for infrastructure provisioning

# ===== LLM INFERENCE OPTIMIZATION =====
# vLLM - High-performance LLM inference with prefix caching (Optional)
# Provides 3-4x throughput improvement with 98.7% cache efficiency
# Requires: CUDA-compatible GPU, Enable in config: llm.vllm.enabled = true
# Installation: Automatically installed via setup.sh
# Note: GPU-required - system will fall back to Ollama if GPU unavailable
vllm>=0.18.0

# ===== GPU-ACCELERATED VECTOR SEARCH (Issue #387) =====
# FAISS - GPU-Accelerated Vector Similarity Search
# Provides 10-100x speedup for vector similarity search operations
# Use faiss-gpu for CUDA support, faiss-cpu for CPU-only fallback
# Note: faiss-gpu requires CUDA toolkit installation
faiss-gpu>=1.7.2
