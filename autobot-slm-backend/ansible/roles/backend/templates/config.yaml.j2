# AutoBot - AI-Powered Automation Platform
# Copyright (c) 2025 mrveiss
# Author: mrveiss
#
# Backend base configuration — generated by Ansible (Issue #2953)
# This file is only written when no config.yaml exists (force: false).
# Edit it directly on the host after first deploy for local customisations.
---

deployment:
  mode: "{{ backend_user_mode | default('single_company') }}"
  host: "{{ backend_redis_host | default('127.0.0.1') }}"
  port: {{ backend_port | default(8001) }}
