{
  "role": "frontend",
  "version": "{{ frontend_version | default(git_commit) | default('unknown') }}",
  "deploy_path": "{{ frontend_install_dir }}/autobot-frontend",
  "service": "autobot-frontend",
  "ansible_playbook": "deploy-frontend.yml",
  "build_steps": [
    "npm install",
    "npm run build into a fresh dist-<id>/, verified for a real index.html, then published by atomically flipping the served `current` symlink to it (roles/_shared/tasks/build_publish_slm_frontend.yml, #15603) — not a flat build-in-place"
  ],
  "depends_on": [],
  "required": true,
  "degraded_without": [],
  "deployed_at": "{{ ansible_facts['date_time'].iso8601 }}"
}
