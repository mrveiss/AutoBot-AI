# SLM Server - Minimal Dependencies
# AutoBot - AI-Powered Automation Platform
# Copyright (c) 2025 mrveiss
#
# System dependencies (install via apt):
#   sudo apt install sshpass  # Required for password-based SSH connections
#

-c ../constraints/shared.txt  # single source of truth for shared versions (#10524); pins transitive numpy

# #16394: previously satisfied only by CI's shared backend venv (autobot-backend/
# requirements.txt's own dependency closure), never declared here. Giving SLM
# tests their own venv (#16394) surfaced them as real collection failures --
# tests/test_bi_dashboard.py needs real numpy (no stub) and a jinja2 fallback
# stub only if the real package is absent; migrations/migrate_system_secrets_
# to_vault_test.py and user_management/services/sso_e2e_test.py need the real
# `sqlite+aiosqlite://` SQLAlchemy driver for in-memory async DB tests.
numpy  # version from constraints/shared.txt
jinja2>=3.1.6,<4.0.0
aiosqlite>=0.22.1  # matches autobot-backend/requirements.txt's pin

# Prometheus metrics (Issue #937: shared monitoring implementation)
prometheus-client>=0.26.0

# Web framework
fastapi>=0.141.1                # SECURITY UPDATE - requires starlette >=1.6.0
starlette>=1.6.0              # explicit pin — avoids python_multipart __all__ ImportError on older starlette (#8275)
uvicorn[standard]>=0.53.0

# Database - PostgreSQL (Issue #786)
# Capped below 2.1 (#17445). A fresh venv from this file took SQLAlchemy 2.1.0
# and 27 tests died inside its own cache-key machinery --
# `TypeError: 'InternalTraversal' object is not callable`, raised in
# `sqlalchemy/sql/_cache_key_cy.py` with no project frame in the failing call.
# 2.0.54 is known good: `requirements-ci/storage.txt` already pins it and that
# venv is healthy on the same commit.
#
# This is a DEFERRAL, not a fix. The cap restores the version CI already pins
# rather than choosing a new state, and it converts an uncontrolled minor bump
# into a recorded one. Migrating to 2.1.x is its own work and is NOT a
# base-unblocking change; the tracking issue is on the cap for whoever lifts it.
# Do not raise this bound without running the SLM suite against 2.1.x first.
sqlalchemy>=2.0.54,<2.1
asyncpg>=0.31.0  # Async PostgreSQL driver for SQLAlchemy
# greenlet is declared explicitly, not left to SQLAlchemy (#17432). SQLAlchemy
# ships it as an `[asyncio]` extra AND, in some releases, under a
# `platform_machine == "x86_64" or ...` marker -- so whether it arrives depends
# on the release the floor resolves to and on the machine resolving it. It
# stopped arriving on 2026-09-24 and every `docker-smoke-test` run failed on
# `main` with `ModuleNotFoundError: No module named 'greenlet'` before the
# container could start. The async engine is a direct requirement of this
# service, so it is declared directly rather than inherited conditionally.
greenlet>=3.1.0  # SQLAlchemy async engine dependency -- see the note above
psycopg2-binary>=2.9.13  # Sync PostgreSQL driver for migrations

# Data validation
pydantic[email]>=2.13.5
pydantic-settings>=2.15.0
typing_extensions>=4.16.0  # For Python 3.8 compatibility

# Authentication
PyJWT[crypto]>=2.14.0
bcrypt>=5.0.0  # Direct bcrypt API — passlib removed (#2669)
python-multipart>=0.0.32          # SECURITY UPDATE - arbitrary file write fix

# Cache (GH #6511: Redis-backed permission cache replaces in-process dict)
redis[asyncio]>=8.1.0

# Async utilities
aiofiles>=25.1.0
websockets>=17.1,<18       # Issue #3098: SLM's own independent floor -- SLM's code never imports websockets directly (it arrives transitively through uvicorn[standard]>=0.52.4, which only requires >=13.0), but this is the version already running in SLM production, so it must not be lowered without cause. #16264 briefly lowered this to match autobot-backend/requirements.txt's <16 langgraph-sdk cap after ci.yml's python-shard job (#13300) installed SLM's tests into the SAME venv as the backend and surfaced a floor conflict there -- that was a CI-venv-sharing artifact, not a reason to downgrade SLM production, and #16391 reverted it in favor of a named exemption in pipeline-scripts/check_dependency_floors.py. #16394 gave the SLM suite its own venv, built from this file, so ci.yml's strict floor check is now scoped per venv and that exemption is gone -- do not re-add it; a floor conflict here again means a venv split broke, not that this line should move.

# Ansible integration
ansible-runner>=2.4.3

# HTTP clients
httpx>=0.28.1  # health checks, general async HTTP calls
# Async HTTP client — genuine runtime dependency, not an optional extra:
# user_management/services/vault_client.py (SSO/system-secrets vault HTTP
# calls), services/a2a_card_fetcher.py, services/reconciler.py's node health
# check, and autobot_shared/retry_mechanism.py's network-retry helper all
# import it (lazily, at call time) — a fresh install was already broken the
# first time any of those code paths ran; this was just never surfaced
# until CI started building the app to verify generated types (#13118).
aiohttp>=3.14.3  # SECURITY UPDATE — matches autobot-backend/requirements.txt pin

# System metrics
psutil>=7.2.2

# SSH connectivity (for connection testing and node management)
paramiko>=5.0.0

# Cron expression parsing (Issue #741 - Scheduled updates)
croniter>=6.2.4

# SSO Integration (Issue #576 Phase 4)
authlib>=1.8.0  # OAuth2/OIDC client
ldap3>=2.9.1  # LDAP/Active Directory
pysaml2>=7.5.5  # SAML 2.0 Service Provider

# 2FA/MFA (Issue #576 Phase 5)
pyotp>=2.10.0  # TOTP generation and verification

# Security: explicit pins to override vulnerable transitive versions
cryptography>=50.0.1  # CVE fix — pulled in by PyJWT[crypto] and pysaml2
# Note: pyopenssl version resolved by pysaml2's dependencies (constraints conflict with >=26.0.0)
# cryptography>=50.0.1 provides primary security coverage
