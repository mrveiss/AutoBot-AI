-e ../autobot-shared

# Core
fastapi>=0.100.0
uvicorn>=0.23.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
pydantic>=2.0
cachetools>=5.0.0
celery>=5.0.0  # Issue #892: Background task processing

# Database
sqlalchemy>=2.0
alembic>=1.12.0
# aiosqlite>=0.19.0  # Included from ../requirements.txt (>=0.21.0)

# AI/ML
openai>=1.0.0
anthropic>=0.18.0
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Include existing requirements
-r ../requirements.txt

# Issue #858: Additional runtime dependencies for Python 3.13
xxhash>=3.6.0          # Hash functions for LLM caching
structlog>=25.5.0      # Structured logging for service auth
llama-index>=0.10.0,<0.11.0    # RAG framework (pinned for API compatibility)
llama-index-llms-ollama>=0.1.0,<0.2.0  # Ollama LLM integration (older API)
llama-index-embeddings-ollama>=0.1.0,<0.2.0  # Ollama embeddings (older API)
llama-index-vector-stores-chroma>=0.1.0,<0.2.0  # ChromaDB vector store (older API)
langchain>=0.1.0,<0.2.0       # LangChain with Tool API (not 0.3+)
markdownify>=0.11.0  # HTML to Markdown conversion
