paramiko>=5.0.0

# Retry logic for resilient operations
tenacity>=9.1.4

# Async SSH for PKI certificate distribution (Issue #166)
asyncssh>=2.24.0

# protobuf — cap and floor, re-derived 2026-08-26 (#15070).
#
# CAP <8.0.0. Both direct requirers admit the whole 7.x line: the pinned
# opentelemetry-proto==1.44.0 declares `protobuf<8.0,>=5.0`, and the transitive
# googleapis-common-protos 1.75.1 declares `protobuf<8.0.0,>=6.33.5`. 8.0 is
# therefore the real incompatibility boundary — nothing here needs <7.0.
#
# The note this replaces said the opposite (cap <7.0, "7.35.1 unsatisfiable",
# "6.33.6 = latest installable"), and the CONSTRAINT was the half that was
# right. That note was accurate when written (#10427) against otel 1.42.x,
# which did require protobuf<7.0. #10589 moved otel to 1.43.0 and #10678
# raised protobuf to >=7.35.1,<8.0.0 the next day; neither touched the
# comment, so it spent two months telling readers to revert a cap that had
# become correct. Recorded here so the next reader does not have to guess.
#
# FLOOR >=7.36.0. Walked up by Dependabot (#14951); it carries forward the
# JSON recursion-depth bypass fix that first set a floor at 5.29.6 (#3933).
# TensorFlow is not a project dependency — the historical <6.0.0 cap it
# justified caused pip conflicts and was removed in #3971.
#
# autobot-backend/requirements.txt `-r`-includes this file and ALSO declares
# protobuf. pip resolves the intersection, so the two lines must stay
# identical; tools/lint/check_requirements_pin_parity.py enforces that.
protobuf>=7.36.0,<8.0.0

# vLLM (GPU inference) is an OPTIONAL extra — NOT a default dependency (#10251).
# It pulls the full CUDA toolkit (cuda-toolkit, nvidia-*, flashinfer, CUDA torch),
# bloating the CPU backend image by several GB and exhausting CI disk. The vLLM
# provider lazy-/guard-imports it (llm_shared/providers/vllm.py, vllm_base.py) and
# is opt-in (llm.vllm.enabled = true), so the default image works without it.
# For GPU deployments that enable vLLM: pip install -r requirements-gpu.txt

# Document parsing libraries for knowledge base
# Supports PDF, Word, Excel, PowerPoint, OpenDocument formats
pypdf>=6.16.1             # PDF parsing - SECURITY UPDATE (improved permission checks)
python-docx>=1.2.0        # Microsoft Word (.docx)
openpyxl>=3.1.5           # Microsoft Excel (.xlsx)
python-pptx>=1.0.2        # Microsoft PowerPoint (.pptx)
odfpy>=1.4.1              # OpenDocument formats (.odt, .ods, .odp, .odg)

# OpenTelemetry - Distributed Tracing (Issue #57, #697)
# Provides request tracing across the 5-VM AutoBot infrastructure
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-proto==1.44.0
opentelemetry-instrumentation-fastapi>=0.65b0
opentelemetry-propagator-b3==1.44.0
# Issue #697: Additional instrumentation for comprehensive tracing
opentelemetry-instrumentation-redis>=0.65b0
opentelemetry-instrumentation-aiohttp-client>=0.65b0

# FAISS - Vector Similarity Search (Issue #387, #856)
# Provides efficient vector similarity search operations
# Using CPU version for Python 3.14 compatibility (GPU version requires conda)
# For GPU acceleration: conda install -c conda-forge faiss-gpu (Python 3.14)
faiss-cpu>=1.15.0
aiosqlite>=0.22.1

# Security - XML parsing protection (Issue #67)
# Prevents XXE (XML External Entity) attacks in threat intelligence parsing
defusedxml>=0.7.1

# MinHash LSH - Locality-Sensitive Hashing for duplicate detection (Issue #659)
# Provides 10-100x speedup for similarity search via O(n) expected complexity
datasketch>=2.0.0
