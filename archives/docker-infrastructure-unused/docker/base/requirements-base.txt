# AutoBot Base Requirements
# Shared dependencies across all Python agent containers
# This consolidates duplicate dependencies from 4+ requirements files

# Core Web Framework (duplicated across all agent containers)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP and Networking (duplicated in 5+ containers)
httpx==0.25.2
requests==2.31.0
websockets==12.0

# Database and Caching (all agents use Redis)
redis==5.0.1
hiredis==2.2.3

# Async and Concurrency (common across agents)
asyncio-mqtt==0.14.0
aiofiles==23.2.1

# Configuration and Environment (all containers)
python-dotenv==1.0.0
pyyaml==6.0.1

# Logging and Monitoring (standardized across containers)
structlog==23.2.0
prometheus-client==0.19.0

# Security (all containers need basic security)
cryptography==41.0.8
passlib==1.7.4

# Utilities (common across all agents)
click==8.1.7
rich==13.7.0
tenacity==8.2.3

# Date/Time handling (used by all agents)
python-dateutil==2.8.2
pytz==2023.3

# JSON and Data Processing (common utilities)
orjson==3.9.10
msgpack==1.0.7

# Health Checks and Status (all containers)
psutil==5.9.6
