# Single source of truth for shared Python dependency versions (#10524).
#
# Every component requirements file references this via:
#     -c <relative-path>/constraints/shared.txt
# Change a version HERE, once. NEVER re-declare a constrained library's version in a
# component requirements file — a CI guard (constraint-drift workflow) fails any PR
# that does, and pip would otherwise resolve different versions per component.
#
# Why a single version is mandatory (not just hygiene): Ansible can co-locate or
# relocate roles (backend, tts-worker, npu-worker, ai-stack) onto one host. Two roles
# with conflicting pins in the same environment make pip unsatisfiable, and numpy ABI
# skew corrupts .npy / pickled embeddings written by one role and read by another.

# numpy unified at 2.x across all roles after graspologic (numpy<2) was removed (#10524).
numpy>=2.5.3,<3.0.0

# openvino floor -- was restated as a literal in 7 independent files (requirements
# files, ansible roles/playbook, a group_var, a setup.py extra and install.sh), so
# every dependabot bump failed CI until the other six were hand-edited (#15408).
# `openvino-dev` has no release compatible with this floor -- see #14447/#14452/#14453.
openvino>=2026.4.0

# ---------------------------------------------------------------------------
# Security floors consolidated here after the 2026-09-23 Dependabot sweep (#17304).
#
# 58 open alerts were six packages. Four were already patched everywhere and the
# alerts were one advisory seen through several resolution paths. The two that were
# genuinely exposed were exposed by DRIFT: a bump applied to some manifests and not
# the rest. That is the failure this file exists to prevent, and these packages were
# simply never brought into it.
# ---------------------------------------------------------------------------

# GH#8323, #17304 — zip bomb + request smuggling. Drifted: 5 manifests were
#              raised to 3.14.3 and 2 were left at 3.13.4, which is how the CVE stayed reachable.
aiohttp>=3.14.3

# #17304 — ReDoS advisory (patched 5.10.0); 5.17.0 is the floor the majority
#              already carried, and one manifest pinned ==5.16.1 behind it.
transformers>=5.17.0

# #17304 — 11 open advisories with NO patched release. Constrained anyway so the
#              version moves in one place the moment upstream ships a fix.
chromadb>=1.5.9,<2.0.0

# #17304 — highest patched floor across 27 advisories (one more has no patch).
nltk>=3.10.3

# #17304 — 10 advisories, highest patched floor 6.16.1; 6.19.0 already in use.
pypdf>=6.19.0

# #17304 — 3 advisories, highest patched floor 50.0.0.
cryptography>=50.0.1
